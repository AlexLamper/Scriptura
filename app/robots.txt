# Block API endpoints from being crawled
User-agent: *
Disallow: /api/

# Allow all other content to be crawled
Allow: /

# Optional: Uncomment the following if you need to slow down crawling (not all bots support Crawl-delay)
# Crawl-delay: 10

# Sitemap location
Sitemap: https://scriptura.cloud/sitemap.xml
